- [ ] Implement guest auth fix: prevent repeated synthetic signups and add in-flight guard
- [ ] If anonymous auth fails, stop falling back to signUp (or throttle it) to avoid 429
- [ ] Improve error surfacing for Supabase 422 responses
- [ ] Run build/lint/tests (npm run build / npm test / npm run lint) and verify auth flow

